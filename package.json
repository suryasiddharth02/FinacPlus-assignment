{
  "name": "qa-automation",
  "version": "1.0.0",
  "description": "Playwright automation — DemoQA Book Store (UI) + reqres.in (API)",
  "scripts": {
    "test": "npx playwright test",
    "test:ui": "npx playwright test tests/ui-bookstore.spec.js",
    "test:api": "npx playwright test tests/api-reqres.spec.js",
    "test:headed": "npx playwright test --headed",
    "report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.6.2"
  }
}
